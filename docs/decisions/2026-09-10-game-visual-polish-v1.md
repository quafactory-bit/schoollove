# Game visual polish V1 — Preview only

User-approved follow-up to PR112 on canonical Preview85686b4. Preserve existing
Home composition, data/projection/XP/referral/auth/relationship boundaries.

Scope: fixed decorative boy/girl duo matching the original mockup, five form-distinct campus
stages using existing level intervals, measured mobile rendering improvement and
user/OS motion control. Two approved empty-ranking/referral microcopy changes only.

User correction during implementation: use the characters from the original
mockup, not three adult alumni. This supersedes the attachment's adult-character
direction. Preserve the original cute proportions and bright expressions; do not
apply the discarded adult-three-person candidate.

Gate: inspect stationary/effects-off stages; verify alpha on white/pastel/navy;
actual React responsive/functional/motion checks; compare production-build lab
390×844 DPR3 with fixed CPU/network, 5 fresh-cache and3 warm-cache runs per version.
Previous3.98s has no recorded LCP element/subparts/throttle/trace and is not a
diagnosed image bottleneck or field-performance result.

No DB/schema/migration/business-rule/env/protection change, real login/signup/
membership/person-search/referral/share/relationship actions, main/Production PR
or release. Existing valid Preview access only; no new bypass links. Respect tool
authorization denial without switching routes to bypass it. Preserve A/B/C and
existing relationships; private checks use synthetic local fixtures.

Follow-up Preview merge is allowed only after required gates. Missing assets,
form/alpha failures, unmet performance budget or blocked live access are reported
explicitly, not hidden behind test counts. Production requires separate approval.

Continuation outcome: the fixed cute duo and two new campus forms passed alpha
QA. BRIGHT_MEMORY arch attempts did not; only four forms are available. Keep the
feature PR Draft and canonical PR112 unchanged. The AVIF Home candidate is a
presentation-only local asset with typed WebP fallback, not a CDN configuration
change. Full results and remaining gates: `docs/GAME_VISUAL_POLISH_V1_RESULT.md`.

## Finish V1 continuation from307ba36

Keep the same branch, original duo and four accepted forms. One precise final
arch edit and one background-only correction are the bounded image attempts;
both returned RGB checkerboards and are excluded. No manual alpha-removal or
alternate image API is authorized. Other local work continues independently.

Performance protocol: one fresh5/warm3 batch on unchanged307ba36 runtime, then
one identical batch on the final candidate. Preserve all historical batches.
Investigate revalidation, selected source/request counts and trace paint work.
Candidate: import the identical AVIF through Next's content-hashed static asset
pipeline, retaining typed preload/WebP fallback and identical bytes; no platform
or CDN configuration changes. Accept no hidden weakening of the no-regression
gate. Additional batches require a stated reason/count before execution.

GitHub installation listing returned an empty list; public reads and duplicate
PR search work (zero matches), but restored PR-write scope is not established.
Do not retry creation until the original connection's permission restoration is
confirmed. No other credentials/tools for PR creation. Feature live access must
be valid for that deployment, without new login, cookie copy or bypass link.

Subsequent explicit user exception: one official Vercel deployment-scoped review
link may be created for the final feature deployment after confirming its SHA,
target and expiry. This overrides only the no-new-review-link clause for that
one deployment. No project-wide protection changes, common bypass secrets,
PR112 link reuse or Production changes. Never persist the URL/cookie in Git,
PR, public report, HAR or trace. If authorization is denied, stop that step.
The existing GitHub connection permission problem remains separate; the current
browser's installation settings entry requires login, which was not performed.

## Remaining gates after c538e02 — explicit independent-layer approval

The user now authorizes a new independent SVG/DOM campus structure on the
unchanged accepted lively art for BRIGHT_MEMORY. This is original frontend
geometry, not pixel extraction, raster editing or background removal of either
rejected arch file. Keep the duo, four existing forms, stage thresholds, motion
controls and immutable Home AVIF. No image-generation retries. Verify equal-size
stationary/effects-off forms, white/pastel/navy compositing, mobile and synchronized
motion. Only this layer and necessary tests/evidence/docs may change.

Preserve all historical performance runs. The latest warm552ms vs original460ms
(+92ms) is explicitly a user design-review decision, not no-regression PASS.
Do not repeatedly optimize/re-measure to erase the difference. Validate the new
layer's actual render/payload impact separately without a new invented target.

Same-branch commit/push is approved. GitHub PR permission and browser network
access remain separate external blockers, not automatically restored by this
approval. No PR retry before confirmed normal restoration; no new review link,
protection change, alternate credential or proxy. No Ready/Preview merge while
any gate remains, and no Production, DB/auth/growth-rule or real-user mutation.

## Accepted V1 and authenticated web finalization

The user accepts the 942a305 gate, cute duo and five rendered forms as V1.
The c538e02 warm median552ms versus PR112460ms (+92ms) and the942a305 shared
decoded JS increase5162 bytes are accepted known costs (PERFORMANCE_TRADEOFF_ACCEPTED),
not NO_REGRESSION_PASS or a new942a305 timing measurement. Preserve all raw evidence.

The subsequent explicit execution approval permits the owner's normal GitHub web
workflow, distinct from the unresolved connector write scope. Draft PR113 was
created through the authenticated owner UI, base preview. No protection bypass.

Actual942a305 public School Hub at1440px revealed the scaled scene's motion button
overlapping its following caption by16.29px. The approved local-regression clause
authorizes only24px desktop caption top margin, preserving scale/art/motion logic.
Reverify the corrected source and final feature deployment before Ready/merge.
No redesign, new performance batch, DB/auth/rules or Production action.
